NAME=umk
VERSION=1.0.0
REVISION=1
URL=https://github.com/user12msd4c/umk/archive/refs/tags/v${VERSION}.tar.gz
DEPENDS=""

build() {
    gcc -O3 -Wall -Wextra -o umk umk.c
}

install() {
    mkdir -p "${1}/usr/bin"
    install -m 755 umk "${1}/usr/bin/"
}
