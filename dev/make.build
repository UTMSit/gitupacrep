NAME=make
VERSION=4.4.1
REVISION=1
URL=https://ftp.gnu.org/gnu/make/make-${VERSION}.tar.gz
DEPENDS=""

build() {
    ./configure --prefix=/usr
    make -j${UPAC_JOBS:-4}
}

upac_install() {
    make install
}
