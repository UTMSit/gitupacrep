NAME="windowmaker"
VERSION="0.96.0"
REVISION=1
URL="https://github.com/window-maker/wmaker/releases/download/wm-${VERSION}/WindowMaker-${VERSION}.tar.gz"
DEPENDS="libX11"

build() {
    ./configure --prefix=/usr
    make -j $UPAC_JOBS
}

upac_install() {
    DESTDIR="$1" make install
}
