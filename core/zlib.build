NAME=zlib
VERSION=1.3.1
REVISION=1
URL=https://zlib.net/zlib-${VERSION}.tar.gz
DEPENDS=""

build() {
    ./configure --prefix=/usr
    make -j${UPAC_JOBS:-4}
}

upac_install() {
    make DESTDIR="${1}" install
}
