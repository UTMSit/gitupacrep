NAME=mpc
VERSION=1.3.1
REVISION=1
URL=https://ftp.gnu.org/gnu/mpc/mpc-${VERSION}.tar.gz
DEPENDS="gmp mpfr"

build() {
    ./configure --prefix=/usr
    make -j${UPAC_JOBS:-4}
}

upac_install() {
    make DESTDIR="${1}" install
}
