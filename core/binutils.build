NAME=binutils
VERSION=2.42
REVISION=1
URL=https://ftp.gnu.org/gnu/binutils/binutils-${VERSION}.tar.xz
DEPENDS=""

build() {
    ./configure --prefix=/usr --disable-werror --disable-gprofng
    make -j${UPAC_JOBS:-4}
}

upac_install() {
    make DESTDIR="${1}" install
}
