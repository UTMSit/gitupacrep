NAME="lightdm"
VERSION="1.32.0"
REVISION=1
URL="https://github.com/canonical/lightdm/releases/download/${VERSION}/lightdm-${VERSION}.tar.gz"
DEPENDS="glib gtk+3"

build() {
    ./configure --prefix=/usr
    make -j $UPAC_JOBS
}

upac_install() {
    DESTDIR="$1" make install
}
