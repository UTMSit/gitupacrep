NAME=expat
VERSION=2.6.3
REVISION=1
URL=https://github.com/libexpat/libexpat/releases/download/R_2_6_3/expat-${VERSION}.tar.xz
DEPENDS=""

build() {
    ./configure --prefix=/usr --disable-static
    make -j${UPAC_JOBS:-4}
}

upac_install() {
    make DESTDIR="${1}" install
}
